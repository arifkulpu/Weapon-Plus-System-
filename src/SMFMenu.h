#pragma once

namespace plugin {
    /**
     * Registers Weapon Plus System sections with SKSE Menu Framework (SMF)
     * if SMF is installed on the user's Skyrim.
     */
    void RegisterSMFMenu();
}
